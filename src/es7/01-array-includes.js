const numbers = [1,2,3,4,5,6,7,8];
const include = numbers.includes(4);
console.log(include);

const names = ['Kevin', 'Paulina', 'Marisol'];
const include1 =  names.includes('kevin'); //cuando se tienen minusculas debemos tranformar el array a para que coincida, igual cuando
//cuando tratamos con distintos tipos de datos
console.log(include1);